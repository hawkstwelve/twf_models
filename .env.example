# Environment Configuration

# Data Sources
DATA_SOURCE=GFS  # Options: GFS, Graphcast, or comma-separated list
GFS_SOURCE=aws   # Options: aws, nomads
GRAPHCAST_API_KEY=  # If using Graphcast API

# Storage
STORAGE_PATH=/var/www/twf_models/images
STORAGE_TYPE=local  # Options: local, s3, spaces

# AWS/DO Spaces (if using)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=twf-models-images

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api
CORS_ORIGINS=https://theweatherforums.com,http://localhost:3000

# Processing
UPDATE_INTERVAL=6  # Hours between updates
MAX_FORECAST_HOUR=120  # Maximum forecast hour to process
FORECAST_HOURS=0,24,48,72  # Key forecast hours for initial release (can expand later)

# Map Generation
MAP_WIDTH=1920
MAP_HEIGHT=1080
MAP_DPI=150
MAP_REGION=pnw  # Options: pnw (Pacific Northwest: WA, OR, ID), us, global, custom

# Logging
LOG_LEVEL=INFO
LOG_FILE=/var/log/twf_models/app.log

# Security (for admin endpoints)
ADMIN_API_KEY=  # Generate a secure random key
